# API Configuration
# ---------------------------
# For APIs requiring authentication (uncomment if needed):
# API_KEY=your_api_key_here

# API header style for authentication (default: bearer)
# Options: bearer, basic, x-access-token, or any custom header name
# API_HEADER_TYPE=x-access-token

# Required: Your API endpoint URL
ENDPOINT_URL=https://api.example.com/v1/data

# Optional: Description of the API (for logs and status)
# API_DESCRIPTION=My API

# Server configuration
# ---------------------------
# Port for the local API server (default: 8000)
PORT=8000

# Logging configuration
# ---------------------------
# Set logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL) (default: INFO)
# LOG_LEVEL=INFO

# Fetcher configuration
# ---------------------------
# Setting to true generates test data instead of calling the API
# TEST_MODE=false

# How often to refresh the cache in seconds (default: 300 = 5 minutes)
# FETCH_INTERVAL_SECONDS=300

# Rate limiting
# ---------------------------
# Enable or disable rate limiting for API calls (default: true)
# RATE_LIMIT_ENABLED=true 